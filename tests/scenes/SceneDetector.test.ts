import { describe, it, expect } from 'vitest';
import { SceneDetector } from '@/core/scenes/SceneDetector';

describe('SceneDetector', () => {
  it('detects scenes from paragraphs', () => {
    const detector = new SceneDetector();
    // TODO
    expect(true).toBe(true);
  });
});
