import { describe, it, expect } from 'vitest';

describe('GeminiDetector', () => {
  it('detects idle state when no indicators present', () => {
    // TODO: JSDOM-based test
    expect(true).toBe(true);
  });
});
